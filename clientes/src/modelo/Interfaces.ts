// src/modelo/Interfaces.ts

// Define a interface LivroMongo
export interface LivroMongo {
    codigo: string;
    titulo: string;
    resumo: string;
    autores: string[];
    codEditora: number;
  }
  